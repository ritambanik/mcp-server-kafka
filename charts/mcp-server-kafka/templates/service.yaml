apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcp-server-kafka.fullname" . }}
  labels:
    {{- include "mcp-server-kafka.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mcp-server-kafka.selectorLabels" . | nindent 4 }}
