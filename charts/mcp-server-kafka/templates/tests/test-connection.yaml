apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mcp-server-kafka.fullname" . }}-test-connection"
  labels:
    {{- include "mcp-server-kafka.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mcp-server-kafka.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
